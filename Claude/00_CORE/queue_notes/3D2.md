# `3D2` — SNAP TRANSFORM, CAPTURE RADIUS, SEAT

> **STATUS** · live · **OWNS** · the row's full record
> **READ IF** · you are about to build the snap, the seat or the mate
> **LAST VERIFIED** · 2026-09-25

⭐ Created 2026-09-25 when `QUEUE.md`'s status cell outgrew a front door. ⛔ The row stays in the
queue; this file is its essay.

## ✅ THE CAPTURE HALF IS BUILT (`D49`, 2026-09-18)

Surface-to-surface, from a convex shape **computed at spawn** (`core/collision_shape.ts`, a GJK
distance) — the owner's preferred answer to *compute it or author a phantom in Blender*, and the
better one: nothing there reads a normal, so **inverted normals cannot affect it**, glTF has no
quads, and a phantom would be a second source of truth for one fact.

⭐⭐ The threshold is **millimetres on the glass**, scaled by camera distance through rule 6's own
tracking factor, so it keeps a constant APPARENT size and is never authored in pixels. It has a
slider.

⚠⚠ **AND IT IS CONVEX, WHICH NOW MATTERS.** `D92` made `objectD` a hollow cylinder; a convex hull
fills the hole, so the capture distance and the white shell treat it as solid. ⛔ A non-convex
capture shape is this row's to decide, and it is the first time the question has had a body to be
asked about.

## ⛔ WHAT IS OWED

The snap, the seat and the mate. ⭐ The mechanism is SPECIFIED (`D46`,
[`../../10_INPUT_TOUCH/spec/APPROACH_AND_MATE.md`](../../10_INPUT_TOUCH/spec/APPROACH_AND_MATE.md)):
capture radius **1.25×**, hold-off **1.1×**, docking by ANGLE, then anti-align + translate-snap.

⛔⛔ **The game cannot assemble anything today**: nothing is SEATED — the ORIENTATION has been a
mate's since `D78` — and §4's `6quater`, the only rule that pushes a `MATE`, is flick-based, which
this input model does not have.

✅ **ANSWERED 2026-09-26 (`D100`): NO GESTURE.** The snap is **automatic** once conditions the
owner will define are met (distance, cone angle, …), so it is not a touchpoint episode of the score;
**unsnap** is an episode, gesture still to define. ⭐⭐ **And a seated Pioneer carries its
followers** — a rigid relationship, which is `3D1`'s tree (parent ≠ root), not a third alignment
mode. ⚠ What that changes for `SNAPSHOT`/`FOLLOW`, the shake and the sway is listed in
[`../../20_GAME_RULES/spec/SCORE.md`](../../20_GAME_RULES/spec/SCORE.md) §4. ⭐ `core/mate_connector.ts`
is built and vectored (`testMate`, `mateResidual`); what is missing is the conditions and the wiring.

## ✅ THE SNAP, THE SEAT AND THE UNSNAP ARE BUILT (2026-09-26) — ⛔ unjudged by a hand

The rules, the table and the costs are
[`../../10_INPUT_TOUCH/spec/ALIGNMENT_RULES.md`](../../10_INPUT_TOUCH/spec/ALIGNMENT_RULES.md)
§11.13. In one line: an aligned Follower whose FollowerFace centre comes within the capture offset
of the PioneerFaceCursor, normals inside the fuchsia cone, lerps onto the cursor and becomes a
CHILD of its Pioneer; it turns about its face, refuses its own translation, and is released by
holding the Pioneer, pressing it, and a rapid zoom-out (tablet) or move (mouse) on the eviction
shake's sliders — then it re-arms only once outside the radius.
⭐ 33 vectors in `tests/snap.test.ts`; 13 mutants red, including *parallel sense*, *no cone*,
*order-agnostic unsnap*, *|Δseparation|*, *seat outlives its link* and *cascade rotates a seat*.
⚠ Still owed here: the approach (the zone), and the mate connector, which stays unwired.
